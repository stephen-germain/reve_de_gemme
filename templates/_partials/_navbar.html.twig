{# <nav class="navbar navbar-expand-lg bg-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('home')}}">Rêves de Gemmes</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav me-auto my-2 my-lg-0 navbar-nav-scroll" style="--bs-scroll-height: 100px;">
				<li class="nav-item">
					<a class="nav-link active" aria-current="page" href="{{ path('home')}}">Accueil</a>
				</li>
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="{{ path('app_all_products')}}" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">Produits</a>
					<ul class="dropdown-menu" aria-labelledby="navbarDropdown"> #}
						{# <li><a href="{{ path('app_all_products')}}" class="dropdown-item">Tous les produits</a></li> #}
						{# <li><a href="{{ path('wristbands_products')}}" class="dropdown-item">Catégorie bracelet</a></li>
						<li><a href="{{ path('necklaces_products')}}" class="dropdown-item">Catégorie collier</a></li>
						<li><a href="{{ path('minerals_products')}}" class="dropdown-item">Catégorie minéraux</a></li>
					</ul>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_all_expositions')}}">Agenda</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_contact')}}">Contact</a>
				</li>
				{% if is_granted('ROLE_ADMIN') %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('admin')}}">Admin</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav> #}

<nav class="navbar navbar-expand-lg bg-light">
	<div class="container-fluid d-block text-center pt-3">
		<button class="navbar-toggler float-end" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<a class="navbar-brand" href="{{ path('home')}}">Rêves de Gemmes</a>
		<div class="collapse navbar-collapse" id="navbarNav">
			<ul class="navbar-nav m-auto py-3" {#navbar-nav-scroll" style="--bs-scroll-height: 100px;#}>
				 <li class="nav-item px-4">
					<a class="nav-link active text-center" aria-current="page" href="{{ path('home')}}">Accueil</a>
				</li>
				<li class="nav-item dropdown px-4">
					<a class="nav-link dropdown-toggle text-center" href="{{ path('app_all_products')}}" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">Produits</a>
					<ul class="dropdown-menu" aria-labelledby="navbarDropdown">
						<li><a href="{{ path('wristbands_products')}}" class="dropdown-item">Catégorie bracelet</a></li>
						<li><a href="{{ path('necklaces_products')}}" class="dropdown-item">Catégorie collier</a></li>
						<li><a href="{{ path('minerals_products')}}" class="dropdown-item">Catégorie minéraux</a></li>
					</ul>
				</li>
				<li class="nav-item px-4">
					<a class="nav-link text-center" href="{{ path('app_all_expositions')}}">Agenda</a>
				</li>
				<li class="nav-item px-4">
					<a class="nav-link text-center" href="{{ path('app_contact')}}">Contact</a>
				</li>
				{% if is_granted('ROLE_ADMIN') %}
					<li class="nav-item px-4">
						<a class="nav-link text-center" href="{{ path('admin')}}">Admin</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
