{% extends 'baseAdmin.html.twig' %}

{% block title %}Gestion des produits{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<div class="container">
    {# <div class="row justify-content-md-center">
        <div class="col-10">
            <div class="row p-5">
                <div class="col-4">
                    <h1>Gestion des produits</h1>
                </div>
            </div>
            <div class="row p-5">
                <div class="col-md-5">
                    {% for bracelet in bracelets %}
                        {% if bracelet.parent == NULL %}
                            <h2>{{ bracelet.name }}</h2>
                        {% else %} 
                            <div class="card col-3" style="width: 18rem;">
                                <h3>{{ bracelet.name }}</h3>
                                {% for product in bracelet.products %}
                                    <img src="{{ asset('img/' ~ product.image)}}" class="card-img-top" alt="..."> 
                                    <div class="card-body">
                                        <h5 class="card-title">{{ product.name }}</h5>
                                        <p class="card-text">{{ product.description}}</p>
                                        <a href="{{ path('update_products', {id:product.id}) }}" class="btn btn-primary">modifier</a>
                                        <a href="{{ path('delete_products', {id:product.id}) }}" class="btn btn-danger">supprimer</a>
                                    </div>
                                {% endfor %}
                            </div>
                        {% endif %}
                    {% endfor %} 
                </div>
            </div>
            <div class="row p-5">
                <div class="col-md-5">
                    {% for collier in colliers %}
                        {% if collier.parent == NULL %}
                            <h3>{{ collier.name }}</h3>
                        {% else %} 
                            <div class="card col-3" style="width: 18rem;">
                                <h3>{{ collier.name }}</h3>
                                {% for product in collier.products %}
                                    <img src="{{ asset('img/' ~ product.image)}}" class="card-img-top" alt="...">
                                    <div class="card-body">
                                        <h5 class="card-title">{{ product.name }}</h5>
                                        <p class="card-text">{{ product.description}}</p>
                                        <a href="{{ path('update_products', {id:product.id}) }}" class="btn btn-primary">modifier</a>
                                        <a href="{{ path('delete_products', {id:product.id}) }}" class="btn btn-danger">supprimer</a>
                                     </div> 
                                {% endfor %}
                            </div>
                        {% endif %}
                    {% endfor %} 
                </div>
            </div>
            <div class="row p-5">
                <div class="col-md-5">
                    {% for pierre in pierres %}
                        {% if pierre.parent == NULL %}
                            <h3>{{ pierre.name }}</h3>
                        {% else %} 
                            <div class="card col-3" style="width: 18rem;">
                                <h3>{{ pierre.name }}</h3>
                                {% for product in pierre.products %}
                                    <img src="{{ asset('img/' ~ product.image)}}" class="card-img-top" alt="...">
                                    <div class="card-body">  
                                        <h5 class="card-title">{{ product.name }}</h5>
                                        <p class="card-text">{{ product.description}}</p>
                                        <a href="{{ path('update_products', {id:product.id}) }}" class="btn btn-primary">modifier</a>
                                        <a href="{{ path('delete_products', {id:product.id}) }}" class="btn btn-danger">supprimer</a>
                                    </div>
                                {% endfor %}      
                            </div>
                        {% endif %}
                    {% endfor %}
                </div>
            </div>
            
            <a href="{{ path('create_products')}}">Ajouter produit</a>

        </div>
    </div> #}

    <div class="row py-5">
        <div class="col-md-12">
            <h1>Gestion des produits</h1>
        </div>
    </div>
    <div class="row">
        {% for bracelet in bracelets %}
            {% if bracelet.parent == NULL %}
                <h2>{{ bracelet.name }}</h2>
            {% else %} 
        <div class="col-md-4 p-3">
            <div class="card m-1" style="width: 18rem;">
                {% for product in bracelet.products %}
                    <img src="{{ asset('img/' ~ product.image) }}" alt="" class="card-img-top">
                    <div class="card-body">
                        <h5 class="card-title">{{ product.name }}</h5>
                        <p class="card-text">{{ product.description}}</p>
                    </div>
                    <div class="card-body">
                        <a href="{{ path('delete_products', {id:product.id}) }}" class="card-link">Supprimer</a>
                        <a href="{{ path('update_products', {id:product.id}) }}"class="card-link">Modifier</a>
                    </div>
                {% endfor %}
            </div>
        </div>
            {% endif %}
        {% endfor %}

        <a href="{{ path('create_products')}}">Ajouter produit</a>
    </div>
</div>

{% endblock %}